<Window x:Class="KillerPDF.MainWindow" Icon="pack://application:,,,/Resources/kp-icon.ico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="KillerPDF" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        Background="#333333">

    <Window.Resources>
        <!-- Killer Tools dark/green theme -->
        <SolidColorBrush x:Key="BgDark" Color="#333333"/>
        <SolidColorBrush x:Key="BgPanel" Color="#3a3a3a"/>
        <SolidColorBrush x:Key="BgHover" Color="#404040"/>
        <SolidColorBrush x:Key="BorderDim" Color="#2e2e2e"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#1ea54c"/>
        <SolidColorBrush x:Key="AccentGreenDim" Color="#0d2a18"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#e0e0e0"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#888888"/>
        <SolidColorBrush x:Key="DangerRed" Color="#ef4444"/>

        <!-- Toolbar button -->
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}"
                                ToolTip="{TemplateBinding ToolTip}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ToolbarButtonAccent" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
        </Style>
        <Style x:Key="ToolbarButtonDanger" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="{StaticResource DangerRed}"/>
        </Style>
        <Style x:Key="ToolbarToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}"
                                ToolTip="{TemplateBinding ToolTip}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentGreenDim}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChromeButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ChromeCloseButton" TargetType="Button" BasedOn="{StaticResource ChromeButton}">
            <Setter Property="Foreground" Value="{StaticResource DangerRed}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource DangerRed}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Thin green scrollbar -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="6"/>
            <Setter Property="MinWidth" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border x:Name="thumbBorder"
                                                                    Background="#551ea54c"
                                                                    CornerRadius="3"
                                                                    Margin="1,0"/>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="thumbBorder" Property="Background" Value="{StaticResource AccentGreen}"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="6"/>
                    <Setter Property="MinHeight" Value="6"/>
                    <Setter Property="Width" Value="Auto"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Dark context menu -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource BgPanel}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDim}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="4">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="20,6,40,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="MenuBd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{TemplateBinding InputGestureText}"
                                           Foreground="{StaticResource TextSecondary}" FontSize="11"
                                           VerticalAlignment="Center" Margin="24,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuBd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Separator">
            <Setter Property="Background" Value="{StaticResource BorderDim}"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="4,4"/>
        </Style>

        <!-- ======================================================== -->
        <!-- Dark ComboBox (zoom dropdown)                           -->
        <!-- ======================================================== -->

        <!-- Toggle button used inside the ComboBox template -->
        <Style x:Key="DarkComboToggle" TargetType="ToggleButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ClickMode" Value="Press"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem inside the dark popup -->
        <Style x:Key="DarkComboItem" TargetType="ComboBoxItem">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBd" Property="Background" Value="{StaticResource AccentGreenDim}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBd" Property="Background" Value="{StaticResource AccentGreenDim}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- The ComboBox itself -->
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="{StaticResource BgPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDim}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Clickable area / border -->
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="18"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- Selected item text (non-editable mode) -->
                                    <ContentPresenter x:Name="ContentSite"
                                                      Grid.Column="0"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="7,0,0,0"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      IsHitTestVisible="False"/>
                                    <!-- Editable text box (shown when IsEditable=True) -->
                                    <TextBox x:Name="PART_EditableTextBox"
                                             Grid.Column="0"
                                             Margin="5,0,0,0"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{StaticResource TextPrimary}"
                                             FontFamily="Consolas"
                                             FontSize="11"
                                             VerticalAlignment="Center"
                                             IsReadOnly="{TemplateBinding IsReadOnly}"
                                             Visibility="Hidden"
                                             Focusable="True"
                                             SelectionBrush="{StaticResource AccentGreenDim}"
                                             SelectionTextBrush="{StaticResource AccentGreen}"
                                             CaretBrush="{StaticResource AccentGreen}"/>
                                    <!-- Chevron arrow -->
                                    <TextBlock Grid.Column="1"
                                               Text="&#xE70D;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="8"
                                               Foreground="{StaticResource TextSecondary}"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"
                                               IsHitTestVisible="False"/>
                                    <!-- Invisible toggle over the whole control -->
                                    <ToggleButton Grid.Column="0" Grid.ColumnSpan="2"
                                                  Style="{StaticResource DarkComboToggle}"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                              RelativeSource={RelativeSource TemplatedParent}}"/>
                                </Grid>
                            </Border>
                            <!-- Dropdown popup -->
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Focusable="False"
                                   PopupAnimation="Fade"
                                   Placement="Bottom">
                                <Border Background="{StaticResource BgPanel}"
                                        BorderBrush="{StaticResource AccentGreenDim}"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Padding="2"
                                        MinWidth="{Binding ActualWidth,
                                                   RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="220">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Disabled">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Page list styles -->
        <Style x:Key="PageListBox" TargetType="ListBox">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDim}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentGreenDim}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- General-purpose dark text button (use on non-icon buttons to bypass WPF default blue hover) -->
        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BgPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDim}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.75"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <Border Grid.Row="0" Background="#222222" BorderBrush="{StaticResource BorderDim}" BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock Text="Killer" FontFamily="Segoe UI" FontWeight="Bold" FontSize="16" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="PDF" FontFamily="Segoe UI" FontWeight="Bold" FontSize="16" Foreground="{StaticResource AccentGreen}"/>
                    <TextBlock Text="" x:Name="FileNameLabel" VerticalAlignment="Center" Margin="16,0,0,0"
                               Foreground="{StaticResource TextSecondary}" FontFamily="Consolas" FontSize="11"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="&#x2015;" Click="MinimizeBtn_Click" Style="{StaticResource ChromeButton}"/>
                    <Button Content="&#x25A1;" Click="MaximizeBtn_Click" Style="{StaticResource ChromeButton}"/>
                    <Button Content="&#x2715;" Click="CloseBtn_Click" Style="{StaticResource ChromeCloseButton}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="#222222" BorderBrush="{StaticResource BorderDim}" BorderThickness="0,0,0,1" Padding="4,2">
            <Grid>
                <!-- File operations -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <!-- File: New / Open / Close / Save / Print -->
                    <Button Content="&#xE7C3;" Style="{StaticResource ToolbarButton}" Click="New_Click" ToolTip="New Blank Document (Ctrl+N)"/>
                    <Button Content="&#xE8DA;" Style="{StaticResource ToolbarButton}" Click="Open_Click" ToolTip="Open (Ctrl+O)"/>
                    <Button x:Name="CloseFileBtn" Content="&#xE711;" Style="{StaticResource ToolbarButton}" Click="CloseFile_Click" ToolTip="Close File (Ctrl+W)" IsEnabled="False"/>
                    <Button x:Name="SaveAsBtn" Content="&#xE74E;" Style="{StaticResource ToolbarButtonAccent}" Click="SaveAs_Click" ToolTip="Save As (Ctrl+Shift+S)"/>
                    <Button Content="&#xE8B9;" Style="{StaticResource ToolbarButton}" Click="SaveFlattened_Click" ToolTip="Save Flattened PDF (rasterize all pages — fully uneditable)"/>
                    <Button Content="&#xE749;" Style="{StaticResource ToolbarButton}" Click="Print_Click" ToolTip="Print (Ctrl+P)"/>
                    <Rectangle Width="1" Fill="{StaticResource BorderDim}" Margin="4,6"/>
                    <!-- Page ops: Merge / Extract -->
                    <Button Content="&#xE710;" Style="{StaticResource ToolbarButton}" Click="Merge_Click" ToolTip="Merge PDFs (combine additional PDFs into this one)"/>
                    <Button Content="&#xE8B1;" Style="{StaticResource ToolbarButton}" Click="Split_Click" ToolTip="Extract Pages (save selected pages to a new PDF)"/>
                    <Rectangle Width="1" Fill="{StaticResource BorderDim}" Margin="4,6"/>
                    <!-- Page ops: Delete / Move -->
                    <Button Content="&#xE8C6;" Style="{StaticResource ToolbarButton}" Click="Delete_Click" ToolTip="Delete Selected Pages"/>
                    <Button Content="&#xE74A;" Style="{StaticResource ToolbarButton}" Click="MoveUp_Click" ToolTip="Move Page Up (reorder)"/>
                    <Button Content="&#xE74B;" Style="{StaticResource ToolbarButton}" Click="MoveDown_Click" ToolTip="Move Page Down (reorder)"/>
                </StackPanel>
                <!-- Edit tools + zoom -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="ToolSelectBtn" Content="&#xE8B3;" Style="{StaticResource ToolbarButton}" Click="ToolSelect_Click" ToolTip="Select Tool - click annotations, drag to select text, double-click to edit"/>
                    <Button x:Name="ToolTextBtn" Content="&#xE8D2;" Style="{StaticResource ToolbarButton}" Click="ToolText_Click" ToolTip="Text Tool - click to add text"/>
                    <Button x:Name="ToolHighlightBtn" Content="&#xED56;" Style="{StaticResource ToolbarButton}" Click="ToolHighlight_Click" ToolTip="Highlight Tool - drag to highlight"/>
                    <Button x:Name="ToolDrawBtn" Content="&#xED63;" Style="{StaticResource ToolbarButton}" Click="ToolDraw_Click" ToolTip="Draw Tool - freehand ink"/>
                    <Button x:Name="ToolCropBtn" Content="&#xE7A8;" Style="{StaticResource ToolbarButton}" Click="ToolCrop_Click" ToolTip="Crop Tool - drag to set crop area, then apply to page(s)"/>
                    <Button x:Name="ToolImageBtn" Content="&#xEB9F;" Style="{StaticResource ToolbarButton}" Click="ToolImage_Click" ToolTip="Image Tool - click to insert an image onto the page"/>
                    <Rectangle Width="1" Fill="{StaticResource BorderDim}" Margin="4,6"/>
                    <Button x:Name="ToolSignatureBtn" Content="&#xEE56;" Style="{StaticResource ToolbarButton}" Click="ToolSignature_Click" ToolTip="Signature - create and place signatures"/>
                    <Rectangle Width="1" Fill="{StaticResource BorderDim}" Margin="4,6"/>
                    <Button Content="&#xE7A7;" Style="{StaticResource ToolbarButton}" Click="Undo_Click" ToolTip="Undo Last (Ctrl+Z)"/>
                    <Button Content="&#xED62;" Style="{StaticResource ToolbarButtonDanger}" Click="ClearAnnotations_Click" ToolTip="Clear All Annotations"/>
                    <Rectangle Width="1" Fill="{StaticResource BorderDim}" Margin="4,6"/>
                    <ToggleButton x:Name="GridViewToggle"
                                  Content="&#xE8A9;"
                                  Style="{StaticResource ToolbarToggleButton}"
                                  IsChecked="True"
                                  IsEnabled="False"
                                  Click="GridViewToggle_Click"
                                  ToolTip="Multi-page grid view"
                                  Margin="0,0,4,0"/>
                    <ComboBox x:Name="ZoomBox" Width="96" Height="24" VerticalAlignment="Center"
                              Margin="0,0,4,0"
                              IsEditable="True" IsReadOnly="True"
                              Style="{StaticResource DarkComboBox}"
                              SelectionChanged="ZoomBox_SelectionChanged" ToolTip="Zoom level (Ctrl+scroll to zoom)">
                        <ComboBoxItem Tag="fitwidth">Fit Width</ComboBoxItem>
                        <ComboBoxItem Tag="fitpage">Fit Page</ComboBoxItem>
                        <ComboBoxItem Tag="0.5">50%</ComboBoxItem>
                        <ComboBoxItem Tag="0.75">75%</ComboBoxItem>
                        <ComboBoxItem Tag="1.0" IsSelected="True">100%</ComboBoxItem>
                        <ComboBoxItem Tag="1.25">125%</ComboBoxItem>
                        <ComboBoxItem Tag="1.5">150%</ComboBoxItem>
                        <ComboBoxItem Tag="2.0">200%</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content -->
        <Grid Grid.Row="2" x:Name="MainContentGrid">
            <Grid.ColumnDefinitions>
                <!-- SidebarCol never collapses to 0 — it shrinks to 24px (the toggle strip width). -->
                <ColumnDefinition x:Name="SidebarCol" Width="180" MinWidth="24" MaxWidth="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar column: inner grid splits content from permanent toggle strip -->
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>        <!-- page list content -->
                    <ColumnDefinition Width="24"/>       <!-- toggle strip — always visible -->
                </Grid.ColumnDefinitions>

                <!-- Page list content (hidden when collapsed) -->
                <Border x:Name="SidebarBorder" Grid.Column="0"
                        Background="#222222"
                        BorderBrush="{StaticResource BorderDim}" BorderThickness="0,0,0,0">
                    <DockPanel>
                        <!-- Sticky header — lives outside the ScrollViewer so it never scrolls away -->
                        <DockPanel DockPanel.Dock="Top" LastChildFill="False" Margin="12,8,8,4">
                            <TextBlock Text="PAGES" DockPanel.Dock="Left"
                                       Foreground="{StaticResource TextSecondary}"
                                       FontFamily="Segoe UI" FontSize="11" FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                            <TextBlock x:Name="PageTotalLabel" DockPanel.Dock="Right"
                                       Text="/ –" VerticalAlignment="Center"
                                       FontFamily="Consolas" FontSize="11"
                                       Foreground="{StaticResource TextSecondary}"
                                       Margin="2,0,0,0"/>
                            <TextBox x:Name="PageJumpBox" DockPanel.Dock="Right"
                                     Width="30" Height="20" VerticalAlignment="Center"
                                     TextAlignment="Center" VerticalContentAlignment="Center"
                                     FontFamily="Consolas" FontSize="11"
                                     Background="{StaticResource BgPanel}"
                                     Foreground="{StaticResource TextPrimary}"
                                     BorderBrush="{StaticResource BorderDim}" BorderThickness="1"
                                     CaretBrush="{StaticResource AccentGreen}"
                                     SelectionBrush="{StaticResource AccentGreenDim}"
                                     IsEnabled="False"
                                     FocusVisualStyle="{x:Null}"
                                     KeyDown="PageJumpBox_KeyDown"
                                     GotFocus="PageJumpBox_GotFocus"
                                     ToolTip="Go to page (Enter to navigate)"
                                     Margin="0,0,2,0">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="BorderBrush" Value="{StaticResource BorderDim}"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                        </DockPanel>
                        <!-- Scrollable page list -->
                        <ScrollViewer x:Name="SidebarScrollViewer"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <ListBox x:Name="PageList" Style="{StaticResource PageListBox}"
                                     BorderThickness="0" SelectionMode="Extended"
                                     ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                     PreviewMouseWheel="PageList_PreviewMouseWheel"
                                     SelectionChanged="PageList_SelectionChanged"
                                     AllowDrop="True" Drop="PageList_Drop" DragOver="PageList_DragOver"
                                     PreviewMouseLeftButtonDown="PageList_PreviewMouseLeftButtonDown"
                                     PreviewMouseMove="PageList_PreviewMouseMove"
                                     MouseRightButtonUp="PageList_MouseRightButtonUp"/>
                        </ScrollViewer>
                    </DockPanel>
                </Border>

                <!-- Toggle strip — always 24px wide, right edge of sidebar column -->
                <Border Grid.Column="1"
                        Background="#333333"
                        BorderBrush="{StaticResource BorderDim}"
                        BorderThickness="1,0,0,0">
                    <Button x:Name="SidebarToggleBtn"
                            Content="&#xE76B;" ToolTip="Collapse sidebar"
                            Style="{StaticResource ToolbarButton}"
                            Width="24" Padding="0" FontSize="10"
                            VerticalAlignment="Top" Margin="0,6,0,0"
                            Click="SidebarToggle_Click"/>
                </Border>
            </Grid>

            <!-- Preview area -->
            <Border Grid.Column="1" Background="{StaticResource BgDark}">
                <Grid>
                    <!-- Drop zone -->
                    <Border x:Name="DropZone" Background="Transparent"
                            AllowDrop="True" Drop="DropZone_Drop" DragOver="DropZone_DragOver"
                            MouseLeftButtonDown="DropZone_Click">
                        <Border BorderBrush="{StaticResource BorderDim}" BorderThickness="2"
                                CornerRadius="8" Margin="40" Padding="40"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Background="Transparent" Style="{x:Null}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="Drop PDF here" FontFamily="Segoe UI" FontSize="20"
                                           Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                                <TextBlock Text="or click to browse" FontFamily="Segoe UI" FontSize="13"
                                           Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>
                    </Border>

                    <!-- PDF page with annotation overlay -->
                    <ScrollViewer x:Name="PagePreviewPanel" Visibility="Collapsed"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto"
                                  Background="Transparent"
                                  PreviewMouseWheel="PagePreview_PreviewMouseWheel"
                                  SizeChanged="PagePreviewPanel_SizeChanged"
                                  PreviewMouseDown="PagePreviewPanel_PreviewMouseDown"
                                  PreviewMouseMove="PagePreviewPanel_PreviewMouseMove"
                                  PreviewMouseUp="PagePreviewPanel_PreviewMouseUp"
                                  AllowDrop="True" Drop="DropZone_Drop" DragOver="DropZone_DragOver">
                        <Border VerticalAlignment="Top" HorizontalAlignment="Center" Padding="12">
                            <Grid x:Name="PageContentGrid">
                                <Grid.LayoutTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Grid.LayoutTransform>
                                <WrapPanel x:Name="PageContentPanel" Orientation="Horizontal">
                                    <!-- Primary page (always present) -->
                                    <Border Background="White" Margin="0,0,12,12">
                                        <Grid>
                                            <Image x:Name="PageImage" RenderOptions.BitmapScalingMode="HighQuality" Stretch="None"/>
                                            <Canvas x:Name="AnnotationCanvas" Background="Transparent" ClipToBounds="True"
                                                    PreviewMouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                                                    MouseMove="Canvas_MouseMove"
                                                    PreviewMouseLeftButtonUp="Canvas_MouseLeftButtonUp"/>
                                        </Grid>
                                    </Border>
                                    <!-- Additional pages rendered dynamically in code-behind -->
                                </WrapPanel>
                            </Grid>
                        </Border>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="3" Background="#222222" BorderBrush="{StaticResource BorderDim}" BorderThickness="0,1,0,0">
            <Grid Margin="16,0">
                <TextBlock x:Name="StatusText" Text="Ready"
                           Foreground="{StaticResource TextSecondary}"
                           FontFamily="Segoe UI" FontSize="11" VerticalAlignment="Center"/>
                <!-- Portable mode indicator (hidden when installed) -->
                <StackPanel x:Name="PortableBadge" Orientation="Horizontal"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="Collapsed">
                    <TextBlock Text="PORTABLE" Foreground="#555555"
                               FontFamily="Consolas" FontSize="9"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button x:Name="InstallBtn" Content="Install KillerPDF..."
                            Click="Install_Click"
                            FontFamily="Segoe UI" FontSize="10"
                            Foreground="{StaticResource AccentGreen}"
                            Background="Transparent"
                            BorderThickness="1" BorderBrush="#2a5a3a"
                            Padding="8,1" Cursor="Hand"
                            VerticalAlignment="Center">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="2" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1a3a28"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Click="ShortcutHelp_Click"
                            Cursor="Hand" VerticalAlignment="Center"
                            Margin="0,0,12,0" FocusVisualStyle="{x:Null}"
                            ToolTip="Keyboard shortcuts (Ctrl+?)">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <TextBlock Text="?"
                                           Foreground="#555555"
                                           FontFamily="Consolas" FontSize="10"
                                           VerticalAlignment="Center"/>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <TextBlock x:Name="VersionLabel" Text="v1.4.1"
                               Foreground="#6a6a6a" FontFamily="Consolas"
                               FontSize="10" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock FontSize="10" VerticalAlignment="Center"
                               FontFamily="Consolas">
                        <Run Text="&#x00A9; 2026 " Foreground="#6a6a6a"/>
                        <Hyperlink NavigateUri="https://thekiller.net"
                                   RequestNavigate="Hyperlink_RequestNavigate"
                                   Foreground="#6a6a6a" TextDecorations="None">
                            <Run Text="Steve the Killer"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Keyboard shortcut overlay -->
        <Grid x:Name="ShortcutOverlay"
              Grid.RowSpan="4"
              Panel.ZIndex="10000"
              Visibility="Collapsed"
              Background="#BB000000"
              MouseLeftButtonDown="ShortcutOverlay_MouseLeftButtonDown">
            <Border Background="#1e1e1e"
                    BorderBrush="{StaticResource AccentGreen}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="28,16,28,22"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="480"
                    MaxWidth="640"
                    MouseLeftButtonDown="ShortcutOverlayCard_MouseLeftButtonDown">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Keyboard Shortcuts"
                                   FontFamily="Segoe UI" FontSize="15" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimary}"
                                   HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <Button Click="ShortcutOverlayClose_Click"
                                HorizontalAlignment="Right" VerticalAlignment="Center"
                                Cursor="Hand" FocusVisualStyle="{x:Null}"
                                Width="24" Height="24">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <TextBlock Text="&#xE711;"
                                               FontFamily="Segoe MDL2 Assets" FontSize="11"
                                               Foreground="{StaticResource TextSecondary}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                                        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>

                    <!-- Column layout -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left column -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="FILE" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+O" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Open" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+W" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Close file" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+N" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="New blank document" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+S" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Save" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+Shift+S" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Save As" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="Ctrl+P" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Print" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>

                            <TextBlock Text="NAVIGATION" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="10" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="&#x2190; / &#x2192; or &#x2191; / &#x2193;" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Previous / next page" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+Scroll" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Zoom in / out" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="Middle drag" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Pan view" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                        </StackPanel>

                        <!-- Right column -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="EDITING" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+Z" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Undo" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Delete" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Delete selected annotation" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Enter / Escape" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Confirm / cancel text or crop" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>

                            <TextBlock Text="SEARCH &amp; SELECT" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="10" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+F" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Find / search" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Enter / Shift+Enter" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Next / previous result" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+A" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Select all text on page" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="Ctrl+C" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="Copy selected text" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>

                            <TextBlock Text="HELP" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="10" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="Ctrl+?" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="This shortcut list" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Film grain overlay - main content area only -->
        <Border x:Name="GrainOverlay"
                Grid.Row="2"
                IsHitTestVisible="False"
                Opacity="0.15"
                Panel.ZIndex="9999">
            <Border.Background>
                <ImageBrush x:Name="GrainBrush"
                            TileMode="Tile"
                            ViewportUnits="Absolute"
                            Viewport="0,0,256,256"
                            Stretch="None"/>
            </Border.Background>
        </Border>
    </Grid>
</Window>
